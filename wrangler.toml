# Cloudflare Pages — static marketing site (epilabs.org)
# Source of truth: website/  (mirrored to site/ and dist/ by npm run build)
#
# Current Cloudflare dashboard:
#   Build command:          (none / or npm run build)
#   Build output directory: site
#
# Recommended (either works after sync):
#   Framework preset:       None
#   Build command:          npm run build
#   Build output directory: site
#   Root directory:         (empty — repo root)

name = "epilabs"
compatibility_date = "2025-01-01"
pages_build_output_dir = "site"
