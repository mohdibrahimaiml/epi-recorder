name: 'EPI Evidence'  
description: 'Generate signed .epi evidence files from AI agent tests'  
  
inputs:  
  epi-dir:  
    description: 'Directory for .epi evidence files'  
    default: './evidence'  
    required: false  
  epi-on-pass:  
    description: 'Keep evidence for passing tests too'  
    default: 'false'  
    required: false  
  
runs:  
  using: 'composite'  
  steps:  
    - name: Install EPI Recorder  
      run: pip install epi-recorder  
      shell: bash  
    - name: Generate evidence  
