Release artifacts and demo bundle
================================

This folder contains helper files for assembling a release for the AGT
interoperability patch (4.0.2).

Files:

- `patch-4.0.2.txt` — human-readable summary of the patch and packaging steps

To build the demo bundle use the packaging script in `scripts/package_demo.py`.
