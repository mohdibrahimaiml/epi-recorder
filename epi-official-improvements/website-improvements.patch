diff --git a/contact.html b/contact.html
index 1354182..d4e3cf5 100644
--- a/contact.html
+++ b/contact.html
@@ -24,7 +24,8 @@
     <meta property="twitter:image" content="https://epilabs.org/assets/logo.png">
 
     <link rel="icon" type="image/png" href="assets/logo.png">
-    <link rel="stylesheet" href="css/style.css?v=2.2">
+    <link rel="canonical" href="https://epilabs.org/contact.html">
+    <link rel="stylesheet" href="css/style.css?v=2.8">
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
     <link
@@ -188,7 +189,7 @@
         </div>
     </footer>
 
-    <script src="js/app.js"></script>
+    <script src="js/app.js" defer></script>
 </body>
 
 </html>
\ No newline at end of file
diff --git a/css/style.css b/css/style.css
index 6e6ab55..308cb74 100644
--- a/css/style.css
+++ b/css/style.css
@@ -89,10 +89,6 @@ html {
     scroll-behavior: smooth;
 }
 
-html {
-    scroll-behavior: smooth;
-}
-
 body {
     font-family: var(--font-sans);
     background: linear-gradient(180deg, var(--bg-primary) 0%, var(--bg-secondary) 50%, var(--bg-primary) 100%);
@@ -137,21 +133,16 @@ h3 {
 }
 
 p {
+    color: var(--color-gray-300);
+    font-size: 1.125rem;
     line-height: 1.8;
     margin-bottom: var(--space-md);
-    color: var(--text-secondary);
 }
 
 h4 {
     font-size: 1.25rem;
 }
 
-p {
-    color: var(--color-gray-300);
-    font-size: 1.125rem;
-    line-height: 1.8;
-}
-
 strong {
     color: var(--color-white);
     font-weight: 600;
@@ -2200,6 +2191,13 @@ html {
         font-size: 1rem;
     }
 
+    /* Minimum 44px touch target for mobile (WCAG 2.1) */
+    .nav-cta a,
+    .btn-primary,
+    .btn-secondary {
+        min-height: 44px;
+    }
+
     .hero-cta {
         flex-direction: column;
         gap: 1rem;
diff --git a/css/terminal.css b/css/terminal.css
index 8f269c2..1cca805 100644
--- a/css/terminal.css
+++ b/css/terminal.css
@@ -97,7 +97,7 @@
 
 /* Content Styles */
 .prompt {
-    color: var(--term-gree);
+    color: var(--term-green);
     margin-right: 8px;
     font-weight: bold;
 }
diff --git a/css/viewer.css b/css/viewer.css
index 771a87c..1964e14 100644
--- a/css/viewer.css
+++ b/css/viewer.css
@@ -412,6 +412,11 @@ body {
     background: rgba(255, 255, 255, 0.02);
 }
 
+.tab-btn:focus-visible {
+    outline: 2px solid var(--color-primary, #8b5cf6);
+    outline-offset: -2px;
+}
+
 .tab-btn.active {
     color: var(--color-primary-light);
     border-bottom-color: var(--color-primary);
diff --git a/how-it-works.html b/how-it-works.html
index 5dafe1e..bba3c36 100644
--- a/how-it-works.html
+++ b/how-it-works.html
@@ -13,7 +13,7 @@
     <meta property="og:title" content="How EPI Works — Cryptographic Proof for AI">
     <meta property="og:description"
         content="Transform any AI workflow into a self-contained, verifiable evidence package.">
-    <meta property="og:image" content="https://epilabs.org/cryptographic_visual.png">
+    <meta property="og:image" content="https://epilabs.org/assets/cryptographic_visual.png">
 
     <!-- Twitter -->
     <meta property="twitter:card" content="summary_large_image">
@@ -21,10 +21,11 @@
     <meta property="twitter:title" content="How EPI Works — Cryptographic Proof for AI">
     <meta property="twitter:description"
         content="Transform any AI workflow into a self-contained, verifiable evidence package.">
-    <meta property="twitter:image" content="https://epilabs.org/cryptographic_visual.png">
+    <meta property="twitter:image" content="https://epilabs.org/assets/cryptographic_visual.png">
 
     <link rel="icon" type="image/png" href="assets/logo.png">
-    <link rel="stylesheet" href="css/style.css?v=2.2">
+    <link rel="canonical" href="https://epilabs.org/how-it-works.html">
+    <link rel="stylesheet" href="css/style.css?v=2.8">
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
     <link
@@ -233,7 +234,7 @@
         </div>
     </footer>
 
-    <script src="js/app.js"></script>
+    <script src="js/app.js" defer></script>
 </body>
 
 </html>
diff --git a/index.html b/index.html
index b68f34c..b6ce21a 100644
--- a/index.html
+++ b/index.html
@@ -13,7 +13,7 @@
     <meta property="og:title" content="EPI LABS | Verifiable Evidence for AI">
     <meta property="og:description"
         content="Self-contained evidence packages that make AI reproducible, verifiable, and legally defensible.">
-    <meta property="og:image" content="https://epilabs.org/epi-file-visual.png">
+    <meta property="og:image" content="https://epilabs.org/assets/logo.png">
 
     <!-- Twitter -->
     <meta property="twitter:card" content="summary_large_image">
@@ -21,10 +21,11 @@
     <meta property="twitter:title" content="EPI LABS | Verifiable Evidence for AI">
     <meta property="twitter:description"
         content="Self-contained evidence packages that make AI reproducible, verifiable, and legally defensible.">
-    <meta property="twitter:image" content="https://epilabs.org/epi-file-visual.png">
+    <meta property="twitter:image" content="https://epilabs.org/assets/logo.png">
 
     <link rel="icon" type="image/png" href="assets/logo.png">
-    <link rel="stylesheet" href="css/style.css?v=2.2">
+    <link rel="canonical" href="https://epilabs.org/">
+    <link rel="stylesheet" href="css/style.css?v=2.8">
     <link rel="stylesheet" href="css/terminal.css">
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
@@ -165,7 +166,7 @@
                         <div class="file-line short" style="opacity: 0.5;"></div>
                     </div>
                     <div class="file-footer">
-                        <div class="seal-icon">🔒</div>
+                        <div class="seal-icon" aria-hidden="true">🔒</div>
                         <div class="seal-info">
                             <div class="seal-text">Signed Evidence</div>
                             <div class="seal-status">✔ Verified</div>
@@ -605,7 +606,6 @@ setup_epi_tracing(service_name="my-agent")
                     target="_blank">📚 Docs</a>
             </div>
         </div>
-        </div>
     </section>
 
     <!-- Footer -->
@@ -648,9 +648,9 @@ setup_epi_tracing(service_name="my-agent")
         </div>
     </footer>
 
-    <script src="js/app.js"></script>
-    <script src="js/terminal-demo.js?v=2.6.0"></script>
-    <script src="js/particles.js"></script>
+    <script src="js/app.js" defer></script>
+    <script src="js/terminal-demo.js?v=2.8.4" defer></script>
+    <script src="js/particles.js" defer></script>
 </body>
 
 </html>
diff --git a/js/app.js b/js/app.js
index ca5e021..7efa1f7 100644
--- a/js/app.js
+++ b/js/app.js
@@ -284,12 +284,7 @@ class InteractiveTerminal {
     }
 }
 
-// Initialize interactive terminals
-document.addEventListener('DOMContentLoaded', () => {
-    document.querySelectorAll('.terminal-interactive').forEach(terminal => {
-        new InteractiveTerminal(terminal);
-    });
-});
+// Note: .terminal-interactive elements are initialized by TerminalDemo in terminal-demo.js
 
 // ============================================
 // FEATURE 2: Hover Tilt Effects
@@ -382,6 +377,8 @@ class CountUp {
 const statObserver = new IntersectionObserver((entries) => {
     entries.forEach(entry => {
         if (entry.isIntersecting && entry.target.dataset.countup) {
+            // Unobserve first to prevent double-firing if animation is slow
+            statObserver.unobserve(entry.target);
             const counter = new CountUp(
                 entry.target,
                 0,
@@ -389,7 +386,6 @@ const statObserver = new IntersectionObserver((entries) => {
                 2000
             );
             counter.animate();
-            statObserver.unobserve(entry.target);
         }
     });
 }, { threshold: 0.5 });
@@ -537,11 +533,22 @@ function sendEmail(event) {
     event.preventDefault();
 
     // Get form values
-    const name = document.getElementById('name').value;
-    const email = document.getElementById('email').value;
-    const company = document.getElementById('company').value || 'Not specified';
+    const name = document.getElementById('name').value.trim();
+    const email = document.getElementById('email').value.trim();
+    const company = document.getElementById('company').value.trim() || 'Not specified';
     const inquiryType = document.getElementById('inquiry_type').value;
-    const message = document.getElementById('message').value;
+    const message = document.getElementById('message').value.trim();
+
+    // Client-side validation with inline feedback
+    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
+    if (!emailRegex.test(email)) {
+        document.getElementById('email').focus();
+        alert('Please enter a valid email address.');
+        return;
+    }
+    if (!name) { document.getElementById('name').focus(); alert('Please enter your name.'); return; }
+    if (!inquiryType) { document.getElementById('inquiry_type').focus(); alert('Please select an inquiry type.'); return; }
+    if (!message) { document.getElementById('message').focus(); alert('Please enter a message.'); return; }
 
     // Create email subject
     const subject = encodeURIComponent('EPI LABS Inquiry: ' + inquiryType);
@@ -561,10 +568,19 @@ function sendEmail(event) {
     );
     // Gmail compose URL - opens Gmail directly
     const gmailUrl = 'https://mail.google.com/mail/?view=cm&fs=1&to=mohdibrahim@epilabs.org&su=' + subject + '&body=' + body;
-    window.open(gmailUrl, '_blank');
+    const popup = window.open(gmailUrl, '_blank');
 
-    // Optional: Clear form after a delay
-    setTimeout(() => {
-        document.getElementById('contactForm').reset();
-    }, 1000);
+    const note = document.querySelector('.form-note');
+    if (!popup || popup.closed || typeof popup.closed === 'undefined') {
+        // Popup was blocked — fall back to mailto and inform the user
+        window.location.href = 'mailto:mohdibrahim@epilabs.org?subject=' + subject + '&body=' + body;
+        if (note) note.textContent = 'Popup blocked — opening your default email client instead.';
+    } else {
+        if (note) note.textContent = 'Gmail opened in a new tab. Just click Send!';
+        // Clear form after a delay
+        setTimeout(() => {
+            document.getElementById('contactForm').reset();
+            if (note) note.textContent = 'Opens Gmail with your message pre-filled. Just click Send!';
+        }, 3000);
+    }
 }
diff --git a/js/particles.js b/js/particles.js
index 700cb8d..5d3013e 100644
--- a/js/particles.js
+++ b/js/particles.js
@@ -39,12 +39,20 @@ class ParticleAnimation {
 
     resize() {
         const hero = this.canvas.parentElement;
-        this.canvas.width = hero.offsetWidth;
-        this.canvas.height = hero.offsetHeight;
+        const w = hero.offsetWidth;
+        const h = hero.offsetHeight;
+        // Guard: skip if parent not yet rendered (width 0)
+        if (w === 0 || h === 0) return;
+        this.canvas.width = w;
+        this.canvas.height = h;
     }
 
     handleResize() {
-        window.addEventListener('resize', () => this.resize());
+        let resizeTimeout;
+        window.addEventListener('resize', () => {
+            clearTimeout(resizeTimeout);
+            resizeTimeout = setTimeout(() => this.resize(), 150);
+        });
     }
 
     handleMouse() {
@@ -68,14 +76,18 @@ class ParticleAnimation {
     }
 
     drawConnections() {
+        const maxDist = 120;
+        const maxDistSq = maxDist * maxDist;
         for (let i = 0; i < this.particles.length; i++) {
             for (let j = i + 1; j < this.particles.length; j++) {
                 const dx = this.particles[i].x - this.particles[j].x;
                 const dy = this.particles[i].y - this.particles[j].y;
-                const distance = Math.sqrt(dx * dx + dy * dy);
+                const distSq = dx * dx + dy * dy;
 
-                if (distance < 120) {
-                    const opacity = (1 - distance / 120) * 0.15;
+                // Use squared distance to avoid costly Math.sqrt in the hot path
+                if (distSq < maxDistSq) {
+                    const distance = Math.sqrt(distSq);
+                    const opacity = (1 - distance / maxDist) * 0.15;
                     this.ctx.beginPath();
                     this.ctx.strokeStyle = 'rgba(10, 132, 255, ' + opacity + ')';
                     this.ctx.lineWidth = 0.5;
@@ -123,7 +135,9 @@ class ParticleAnimation {
     }
 }
 
-// Initialize on page load
+// Initialize on page load — skip on mobile to save battery
 document.addEventListener('DOMContentLoaded', () => {
-    new ParticleAnimation('particle-canvas');
+    if (window.matchMedia('(hover: hover) and (pointer: fine)').matches) {
+        new ParticleAnimation('particle-canvas');
+    }
 });
diff --git a/js/terminal-demo.js b/js/terminal-demo.js
index a53f843..8d01e3d 100644
--- a/js/terminal-demo.js
+++ b/js/terminal-demo.js
@@ -44,7 +44,7 @@ class TerminalDemo {
 
         // Initial welcome message - print FIRST before creating command line
         this.print([
-            { text: 'EPI Portable Environment v2.7.0', color: 'text-dim' },
+            { text: 'EPI Portable Environment v2.8.5', color: 'text-dim' },
             { text: 'Type "help" for a list of commands.', color: 'text-dim' },
             { text: '' }
         ]);
@@ -129,8 +129,6 @@ class TerminalDemo {
             const cmd = this.inputLine.value.trim();
             this.history.push(cmd);
             this.historyIndex = this.history.length;
-            this.history.push(cmd);
-            this.historyIndex = this.history.length;
             this.execute(cmd, true); // User always focuses
         } else if (e.key === 'ArrowUp') {
             e.preventDefault();
@@ -221,7 +219,7 @@ class TerminalDemo {
 
         if (subcmd === 'run' || subcmd === 'record') {
             if (!args[1]) {
-                this.print([{ text: 'accusing: missing argument <file>', color: 'text-red' }]);
+                this.print([{ text: 'usage: missing argument <file>', color: 'text-red' }]);
                 return;
             }
             await this.simulateRecording(args[1]);
@@ -239,7 +237,7 @@ class TerminalDemo {
 
     async simulateRecording(filename) {
         const steps = [
-            { text: `→ Starting EPI Recorder v2.7.0`, delay: 200 },
+            { text: `→ Starting EPI Recorder v2.8.5`, delay: 200 },
             { text: `  Target: ${filename}`, delay: 100 },
             { text: `  Environment: Python 3.11.4 | Windows 10`, color: 'text-dim', delay: 300 },
             { text: `● Recording active... (PID: 1420)`, color: 'text-green', delay: 400 },
diff --git a/js/viewer.js b/js/viewer.js
index 5d1e898..519432f 100644
--- a/js/viewer.js
+++ b/js/viewer.js
@@ -37,6 +37,14 @@ function initializeApp() {
     // Drag and drop
     const dropZone = document.getElementById('drop-zone');
 
+    // Keyboard activation for the drop zone (accessibility)
+    dropZone.addEventListener('keydown', (e) => {
+        if (e.key === 'Enter' || e.key === ' ') {
+            e.preventDefault();
+            fileInput.click();
+        }
+    });
+
     ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
         dropZone.addEventListener(eventName, preventDefaults, false);
     });
@@ -74,7 +82,14 @@ function showState(stateName) {
 
 async function handleFile(file) {
     if (!file.name.endsWith('.epi')) {
-        alert('Please select a .epi file.');
+        alert('Please select a valid .epi file.');
+        return;
+    }
+    // Guard: check the file starts with PK (ZIP magic bytes)
+    const header = await file.slice(0, 2).arrayBuffer();
+    const magic = new Uint8Array(header);
+    if (magic[0] !== 0x50 || magic[1] !== 0x4B) {
+        alert('This file does not appear to be a valid .epi archive. Expected a ZIP-based format.');
         return;
     }
 
@@ -277,21 +292,24 @@ function displayVerifiedEvidence(result) {
     container.innerHTML = '';
 
     if (result.viewerHtml) {
+        // Show spinner while iframe loads
+        container.innerHTML = '<div style="display:flex;align-items:center;justify-content:center;height:200px;color:#64748b;gap:0.5rem;"><svg style="animation:spin 1s linear infinite" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/></svg>Loading viewer...</div>';
+
         const iframe = document.createElement('iframe');
         iframe.sandbox = "allow-scripts allow-popups allow-forms";
-        container.appendChild(iframe);
+        iframe.style.cssText = 'width:100%;height:100%;border:none;display:none;';
 
         // Inject layout fixes into the embedded HTML
         // This forces the content to fit the iframe and handle scrolling
         let htmlContent = result.viewerHtml;
         const layoutFix = `
             <style>
-                html, body { 
-                    height: 100% !important; 
-                    width: 100% !important; 
-                    margin: 0 !important; 
-                    padding: 0 !important; 
-                    overflow: auto !important; 
+                html, body {
+                    height: 100% !important;
+                    width: 100% !important;
+                    margin: 0 !important;
+                    padding: 0 !important;
+                    overflow: auto !important;
                 }
                 /* Ensure terminal/content containers fill space */
                 #terminal, .terminal, .xterm-viewport {
@@ -309,7 +327,17 @@ function displayVerifiedEvidence(result) {
 
         // Use Blob URL to safely load content
         const blob = new Blob([htmlContent], { type: 'text/html' });
-        iframe.src = URL.createObjectURL(blob);
+        const blobUrl = URL.createObjectURL(blob);
+
+        iframe.onload = () => {
+            container.innerHTML = '';
+            container.appendChild(iframe);
+            iframe.style.display = '';
+            URL.revokeObjectURL(blobUrl);
+        };
+
+        iframe.src = blobUrl;
+        container.appendChild(iframe);
     } else {
         container.innerHTML = '<div style="padding:20px; text-align:center; color:#666;">No embedded viewer found.</div>';
     }
diff --git a/simulation.html b/simulation.html
index 1d898c5..8244dad 100644
--- a/simulation.html
+++ b/simulation.html
@@ -6,7 +6,8 @@
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Live Simulation — EPI LABS</title>
     <link rel="icon" type="image/png" href="assets/logo.png">
-    <link rel="stylesheet" href="css/style.css?v=2.6">
+    <link rel="canonical" href="https://epilabs.org/simulation.html">
+    <link rel="stylesheet" href="css/style.css?v=2.8">
     <link rel="stylesheet" href="css/terminal.css">
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
@@ -181,7 +182,7 @@
                                 <span class="control-dot dot-yellow"></span>
                                 <span class="control-dot dot-green"></span>
                             </div>
-                            <div class="terminal-title">epi-recorder v2.7.2</div>
+                            <div class="terminal-title">epi-recorder v2.8.5</div>
                         </div>
                         <div class="terminal-body">
                             <!-- Content injected by terminal-demo.js -->
@@ -192,8 +193,8 @@
         </div>
     </section>
 
-    <script src="js/app.js"></script>
-    <script src="terminal-demo.js?v=2.6.0"></script>
+    <script src="js/app.js" defer></script>
+    <script src="js/terminal-demo.js?v=2.8.4" defer></script>
     <script>
         // Custom Logic to link Terminal to Stepper
         document.addEventListener('DOMContentLoaded', () => {
diff --git a/sw.js b/sw.js
index c57b31e..7aefed3 100644
--- a/sw.js
+++ b/sw.js
@@ -32,7 +32,7 @@ self.addEventListener('activate', (event) => {
 
 self.addEventListener('fetch', (event) => {
     // Network-First Strategy for HTML to ensure we always get the latest layout
-    if (event.request.headers.get('accept').includes('text/html')) {
+    if ((event.request.headers.get('accept') || '').includes('text/html')) {
         event.respondWith(
             fetch(event.request)
                 .then((response) => {
diff --git a/technology.html b/technology.html
index bb4c0f6..43d91ba 100644
--- a/technology.html
+++ b/technology.html
@@ -12,10 +12,11 @@
     <meta property="og:url" content="https://epilabs.org/technology.html">
     <meta property="og:title" content="EPI Architecture — The Trustless Verification Stack">
     <meta property="og:description" content="How we achieved offline, zero-knowledge verification for AI workflows.">
-    <meta property="og:image" content="https://epilabs.org/trust_network.png">
+    <meta property="og:image" content="https://epilabs.org/assets/trust_network.png">
 
     <link rel="icon" type="image/png" href="assets/logo.png">
-    <link rel="stylesheet" href="css/style.css?v=2.2">
+    <link rel="canonical" href="https://epilabs.org/technology.html">
+    <link rel="stylesheet" href="css/style.css?v=2.8">
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
     <link
@@ -284,7 +285,7 @@ vizFrame.src = URL.createObjectURL(blob);
         </div>
     </footer>
 
-    <script src="js/app.js"></script>
+    <script src="js/app.js" defer></script>
 </body>
 
 </html>
diff --git a/use-cases.html b/use-cases.html
index 277cda8..092acae 100644
--- a/use-cases.html
+++ b/use-cases.html
@@ -13,7 +13,7 @@
     <meta property="og:title" content="EPI Use Cases — AI Compliance & Verification">
     <meta property="og:description"
         content="Discover how EPI solves the AI trust crisis in Healthcare, Finance, Insurance, and Legal industries.">
-    <meta property="og:image" content="https://epilabs.org/trust_network.png">
+    <meta property="og:image" content="https://epilabs.org/assets/trust_network.png">
 
     <!-- Twitter -->
     <meta property="twitter:card" content="summary_large_image">
@@ -21,10 +21,11 @@
     <meta property="twitter:title" content="EPI Use Cases — AI Compliance & Verification">
     <meta property="twitter:description"
         content="Discover how EPI solves the AI trust crisis in Healthcare, Finance, Insurance, and Legal industries.">
-    <meta property="twitter:image" content="https://epilabs.org/trust_network.png">
+    <meta property="twitter:image" content="https://epilabs.org/assets/trust_network.png">
 
     <link rel="icon" type="image/png" href="assets/logo.png">
-    <link rel="stylesheet" href="css/style.css?v=2.6">
+    <link rel="canonical" href="https://epilabs.org/use-cases.html">
+    <link rel="stylesheet" href="css/style.css?v=2.8">
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
     <link
@@ -379,7 +380,7 @@
         </div>
     </footer>
 
-    <script src="js/app.js"></script>
+    <script src="js/app.js" defer></script>
 </body>
 
 </html>
\ No newline at end of file
diff --git a/verify.html b/verify.html
index 1b831ff..9578d96 100644
--- a/verify.html
+++ b/verify.html
@@ -6,13 +6,13 @@
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Verify Evidence | EPI LABS</title>
     <!-- Dependencies (CDNs) -->
-    <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
-    <script src="https://cdn.jsdelivr.net/npm/cbor-js@0.1.0/cbor.min.js"></script>
+    <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js" defer></script>
+    <script src="https://cdn.jsdelivr.net/npm/cbor-js@0.1.0/cbor.min.js" defer></script>
     <!-- Noble Ed25519 -->
-    <script src="https://unpkg.com/@noble/ed25519@2.0.0/lib/index.js"></script>
+    <script src="https://unpkg.com/@noble/ed25519@2.0.0/lib/index.js" defer></script>
 
     <!-- Custom System Styles -->
-    <link rel="stylesheet" href="css/style.css?v=2.6">
+    <link rel="stylesheet" href="css/style.css?v=2.8">
     <link rel="stylesheet" href="css/terminal.css">
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
@@ -345,27 +345,27 @@
     </main>
 
     <!-- UI Modals -->
-    <div id="timelineModal" class="glass-modal">
+    <div id="timelineModal" class="glass-modal" role="dialog" aria-modal="true" aria-labelledby="timelineModalTitle">
         <div class="glass-modal-content">
             <div class="modal-header">
                 <div>
-                    <h3 style="margin-bottom: 0;">Evidence Timeline</h3>
+                    <h3 id="timelineModalTitle" style="margin-bottom: 0;">Evidence Timeline</h3>
                     <p style="font-size: 0.8rem; margin:0;">Granular execution trace from <code style="padding: 0 0.25rem; font-size: 1em;">steps.jsonl</code></p>
                 </div>
-                <button class="btn-secondary" style="padding: 0.25rem 0.5rem; border:none;" onclick="closeTimeline()">✕</button>
+                <button class="btn-secondary" style="padding: 0.25rem 0.5rem; border:none;" onclick="closeTimeline()" aria-label="Close timeline modal">✕</button>
             </div>
             <div class="modal-body" id="timelineContent"></div>
         </div>
     </div>
 
-    <div id="filesModal" class="glass-modal">
+    <div id="filesModal" class="glass-modal" role="dialog" aria-modal="true" aria-labelledby="filesModalTitle">
         <div class="glass-modal-content" style="max-width: 600px;">
             <div class="modal-header">
                 <div>
-                    <h3 style="margin-bottom: 0;">File Manifest</h3>
+                    <h3 id="filesModalTitle" style="margin-bottom: 0;">File Manifest</h3>
                     <p style="font-size: 0.8rem; margin:0;">Cryptographic inventory (SHA-256)</p>
                 </div>
-                <button class="btn-secondary" style="padding: 0.25rem 0.5rem; border:none;" onclick="closeFiles()">✕</button>
+                <button class="btn-secondary" style="padding: 0.25rem 0.5rem; border:none;" onclick="closeFiles()" aria-label="Close file manifest modal">✕</button>
             </div>
             <div class="modal-body" style="padding: 0;">
                 <table style="width: 100%; text-align: left; border-collapse: collapse;">
@@ -396,6 +396,12 @@
         const resultPhase = document.getElementById('resultPhase');
         const launchVizBtn = document.getElementById('launchVizBtn');
 
+        // CDN dependency check — show friendly error if scripts failed to load
+        if (typeof JSZip === 'undefined') {
+            const dz = document.getElementById('dropZone');
+            if (dz) dz.innerHTML = '<p style="color:var(--color-warning,#f59e0b);text-align:center;padding:2rem;">⚠️ Required libraries failed to load (CDN unavailable).<br>Please check your internet connection and reload the page.</p>';
+        }
+
         // Drag & Drop Handlers
         dropZone.addEventListener('click', () => fileInput.click());
         dropZone.addEventListener('dragover', (e) => { e.preventDefault(); dropZone.classList.add('dragover'); });
@@ -407,11 +413,41 @@
         });
         fileInput.addEventListener('change', (e) => processFile(e.target.files[0]));
 
-        // Global UI Toggles
-        window.openTimeline = () => document.getElementById('timelineModal').classList.add('active');
-        window.closeTimeline = () => document.getElementById('timelineModal').classList.remove('active');
-        window.openFiles = () => document.getElementById('filesModal').classList.add('active');
-        window.closeFiles = () => document.getElementById('filesModal').classList.remove('active');
+        // Global UI Toggles with focus management
+        window.openTimeline = () => {
+            const m = document.getElementById('timelineModal');
+            m.classList.add('active');
+            const btn = m.querySelector('button');
+            if (btn) btn.focus();
+        };
+        window.closeTimeline = () => {
+            document.getElementById('timelineModal').classList.remove('active');
+        };
+        window.openFiles = () => {
+            const m = document.getElementById('filesModal');
+            m.classList.add('active');
+            const btn = m.querySelector('button');
+            if (btn) btn.focus();
+        };
+        window.closeFiles = () => {
+            document.getElementById('filesModal').classList.remove('active');
+        };
+
+        // Close modals on Escape key
+        document.addEventListener('keydown', (e) => {
+            if (e.key === 'Escape') {
+                window.closeTimeline();
+                window.closeFiles();
+            }
+        });
+
+        // Close modals on backdrop click
+        document.getElementById('timelineModal').addEventListener('click', (e) => {
+            if (e.target === e.currentTarget) window.closeTimeline();
+        });
+        document.getElementById('filesModal').addEventListener('click', (e) => {
+            if (e.target === e.currentTarget) window.closeFiles();
+        });
         
         window.resetUI = () => {
             resultPhase.classList.add('hidden');
@@ -462,15 +498,30 @@
                     }).filter(Boolean);
                 }
 
-                // Setup Offline Viewer Button
+                // Setup Offline Viewer Button — render in sandboxed iframe (not new tab)
+                // to prevent XSS from untrusted viewer.html content in the .epi archive
                 const viewerFile = zip.file("viewer.html");
                 if (viewerFile) {
                     launchVizBtn.classList.remove('hidden');
                     launchVizBtn.onclick = async () => {
                         const htmlContent = await viewerFile.async("string");
                         const blob = new Blob([htmlContent], { type: 'text/html' });
-                        // Open strictly contained isolated page
-                        window.open(URL.createObjectURL(blob), '_blank', 'noopener,noreferrer');
+                        const blobUrl = URL.createObjectURL(blob);
+                        // Use sandboxed iframe: allow-scripts only, no allow-same-origin
+                        // This prevents the embedded page from accessing parent DOM or cookies
+                        const container = document.getElementById('vizContainer') || (() => {
+                            const el = document.createElement('div');
+                            el.id = 'vizContainer';
+                            el.style.cssText = 'position:fixed;inset:0;z-index:3000;background:#000;display:flex;flex-direction:column;';
+                            el.innerHTML = '<div style="display:flex;justify-content:flex-end;padding:0.5rem;background:#0f172a;"><button onclick="document.getElementById(\'vizContainer\').remove();URL.revokeObjectURL(this.dataset.url)" data-url="' + blobUrl + '" style="background:#334155;color:#fff;border:none;padding:0.5rem 1rem;border-radius:6px;cursor:pointer;">Close ✕</button></div>';
+                            document.body.appendChild(el);
+                            return el;
+                        })();
+                        const iframe = document.createElement('iframe');
+                        iframe.sandbox = 'allow-scripts allow-forms allow-popups';
+                        iframe.style.cssText = 'flex:1;width:100%;border:none;';
+                        iframe.src = blobUrl;
+                        container.appendChild(iframe);
                     };
                 }
 
@@ -707,8 +758,8 @@
         </div>
     </footer>
 
-    <script src="js/app.js"></script>
-    <script src="js/particles.js"></script>
+    <script src="js/app.js" defer></script>
+    <script src="js/particles.js" defer></script>
 </body>
 
 </html>
diff --git a/viewer.html b/viewer.html
index 35f07de..b7e6b31 100644
--- a/viewer.html
+++ b/viewer.html
@@ -8,12 +8,13 @@
     <meta name="description" content="Open and inspect any .epi recording directly in your browser. No install required. Integrity checked with SHA-256, signatures verified with Ed25519.">
 
     <!-- Dependencies -->
-    <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
+    <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js" defer></script>
 
     <!-- EPI Labs Design System -->
-    <link rel="stylesheet" href="css/style.css?v=2.6">
+    <link rel="stylesheet" href="css/style.css?v=2.8">
     <link rel="stylesheet" href="css/viewer.css?v=2.8">
     <link rel="icon" type="image/png" href="assets/logo.png">
+    <link rel="canonical" href="https://epilabs.org/viewer.html">
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
     <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet">
@@ -472,15 +473,17 @@
             <h1 class="viewer-hero-title">Inspect any <span class="text-gradient">.epi</span> recording</h1>
             <p class="viewer-hero-sub">Drop a file below to inspect trust, integrity, policy, analysis, and review. Nothing leaves your browser.</p>
 
-            <div id="drop-zone" class="drop-zone">
-                <span class="drop-icon">📂</span>
+            <div id="drop-zone" class="drop-zone"
+                 role="button" tabindex="0"
+                 aria-label="Drop zone: drag and drop a .epi file here, or press Enter to browse">
+                <span class="drop-icon" aria-hidden="true">📂</span>
                 <h3>Drag &amp; drop a .epi file</h3>
                 <p>or click to browse your computer.<br>Works offline and keeps the file on your machine.</p>
-                <button class="btn-open-file" id="btn-open-file">
-                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
+                <button class="btn-open-file" id="btn-open-file" aria-label="Open file picker to choose a .epi file">
+                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
                     Choose .epi file
                 </button>
-                <input type="file" id="file-input" accept=".epi" style="display:none">
+                <input type="file" id="file-input" accept=".epi" style="display:none" aria-hidden="true">
             </div>
 
             <p class="trust-note">
@@ -604,7 +607,7 @@
 
 </div><!-- /#app -->
 
-<script src="js/viewer.js?v=2.8"></script>
-<script src="js/app.js"></script>
+<script src="js/viewer.js?v=2.8" defer></script>
+<script src="js/app.js" defer></script>
 </body>
 </html>
diff --git a/vision.html b/vision.html
index 450d2ac..6a81435 100644
--- a/vision.html
+++ b/vision.html
@@ -13,7 +13,7 @@
     <meta property="og:title" content="EPI Vision — The Future of AI Trust">
     <meta property="og:description"
         content="Our roadmap to build the trust layer for the AI economy. From local verification to decentralized proof networks.">
-    <meta property="og:image" content="https://epilabs.org/trust_network.png">
+    <meta property="og:image" content="https://epilabs.org/assets/trust_network.png">
 
     <!-- Twitter -->
     <meta property="twitter:card" content="summary_large_image">
@@ -21,10 +21,11 @@
     <meta property="twitter:title" content="EPI Vision — The Future of AI Trust">
     <meta property="twitter:description"
         content="Our roadmap to build the trust layer for the AI economy. From local verification to decentralized proof networks.">
-    <meta property="twitter:image" content="https://epilabs.org/trust_network.png">
+    <meta property="twitter:image" content="https://epilabs.org/assets/trust_network.png">
 
     <link rel="icon" type="image/png" href="assets/logo.png">
-    <link rel="stylesheet" href="css/style.css?v=2.6">
+    <link rel="canonical" href="https://epilabs.org/vision.html">
+    <link rel="stylesheet" href="css/style.css?v=2.8">
     <style>
         /* Vision Page Specific Styles */
         .shift-grid {
@@ -363,7 +364,7 @@
         </div>
     </footer>
 
-    <script src="js/app.js"></script>
+    <script src="js/app.js" defer></script>
 </body>
 
 </html>
\ No newline at end of file
